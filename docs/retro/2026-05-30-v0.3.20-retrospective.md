# Retrospective — v0.3.20

**Date:** 2026-05-30
**Milestone:** feat/v0.3.20 — sudo no-TTY fallback
**PR:** #29 — merged to main (`2f46d4beb3b9348bd274e36f91e51be50c3d15dd`)
**Participants:** Claude, Codex, Copilot

## What Went Well
- Fix was minimal (2 lines) and targeted — no contract changes
- k3d-manager subtree pull happened immediately in the same session
- Copilot caught the test gap; test fix was straightforward

## What Went Wrong
- BATS test mocks used `"$@"` without stripping flags — broke when `-n` was introduced
- Haiku subagent made an extra unplanned commit (`52ccf51`) refining the condition — spec was clear enough, but the agent added its own interpretation

## Process Rules Added
None new this milestone.

## Decisions Made
- sudo mock pattern: `while [[ $# -gt 0 && "$1" == -* ]]; do shift; done; "$@"` — strips all flags before executing; use this in all future sudo mocks in system.bats

## Theme
A focused 2-line fix to `_run_command_resolve_sudo` that makes `make up` survivable without a TTY. The main friction was test infrastructure: the sudo mock needed to mirror real sudo's flag-stripping behavior. Once fixed, CI went green on the first re-run.
